"""Utility package namespace for Cube Capture.

Modules import helpers directly (e.g. ``from .render_setup import ensure_flat_lighting``)
to keep dependencies explicit and avoid unused re-exports.
"""
